1. Bug: rotated objects bounds are not rotated too. They still react to hover on empty place where they were rotated from.
2. Bug: objects which behind some objects are still having a hover (hit test still passes even they're under some object or some part of the object under some object).
3. Blur effect is not working, try writing shaders.
4. Add z-index which works like a web z-index. Safe with hit testing and others.
5. Add Resource management system. Add new cmake function: ouif_add_file(target executable/app; id (int); file path;). It'll add that file as a bytearray to the executable, and manages those files easily using ids. pseudo: Resources.load(id). And methods which requires external source can apply this too as a next option, not only option.
6. Add drag drop api. Events, methods, functions etc. Not visual, because its user's problem.
7. Add new Overlay widget. It used to act like frame layout in android, but a little different, when you place it somewhere, it doesnt take space, doesnt perform there in layout, its like adding another layer, but it doesnt affect on backdrop, just places widgets in its own area, if its flexes, max is parent's width, height. If multiple overlays are added in the widget, z-index will help to form them.
8. We should implement, widget removing system too. Removing is runtime thing only. Memory safe.
9. Add these properties to widget, if its exists, dont, or enhance: enabled, set_enabled, get_enabled (bool, it defines if widget is visual only or no, if false, widget doesnt react to anything except just being visible); visibility, set/get_visibilty (bool, defines if its visible, if false, it makes widget invisible, not only visually hides from the layout too, doesn performs in that layer's spacing, layout and others, safe to make visible again.), ghost, setter getters (bool, works okay, but its not hittable, when you hit it, the layer under it hits, not self just like ghost, difference between `enabled` is, when you hit the enabled, hit doesnt passes through it, hit just disappears)
10. Let developers own main loop, like they might make low level programs. And add there our app's methods that glues to our framework and dev's program, so devs dont have to write every loop updates of our framework themselves.